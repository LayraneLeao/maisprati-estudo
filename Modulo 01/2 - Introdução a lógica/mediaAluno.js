// Calcular a média final de um aluno com base em duas provas (P1 e P2), exibir o resultado final pro usuário

const p1 = 10
const p2 = 8
const media = (p1 + p2) / 2

console.log(`\nA média do aluno é: ${media}\n`)