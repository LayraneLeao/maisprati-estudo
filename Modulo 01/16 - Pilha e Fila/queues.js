/*
Uma queue (fila) é uma estrutura de dados usada para armazenar elementos em ordem sequencial, seguindo o princípio:

FIFO – First In, First Out
(Primeiro a entrar, primeiro a sair)

*/
const prompt = require("prompt-sync")()
let array = []

function preencherArray(array){
    array = Number(prompt("Informe um número: "))

    
}